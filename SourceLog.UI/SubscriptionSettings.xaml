<UserControl x:Class="SourceLog.SubscriptionSettings"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 mc:Ignorable="d">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto" />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
        <Label Style="{StaticResource LabelStyle}">URL: </Label>
        <TextBox Name="txtUrl" Grid.Column="1" Margin="6" Style="{StaticResource TextBoxStyle}" 
				AcceptsReturn="True" Height="Auto" />
	</Grid>
</UserControl>
